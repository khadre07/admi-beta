# >=1.61 : l'horloge de l'en-tête utilise st.iframe, absent des versions plus
# anciennes (le déploiement cloud installe la version résolue depuis ce fichier).
streamlit>=1.61
plotly>=6.0
pandas>=2.2
openpyxl>=3.1
SQLAlchemy>=2.0
# PostgreSQL (optionnel) : décommentez pour un serveur multi-utilisateurs
# psycopg2-binary>=2.9
streamlit-calendar>=1.0
python-docx>=1.1
pymupdf>=1.24
reportlab>=4.0
